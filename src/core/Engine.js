/**
 * WebGL 렌더링 엔진의 메인 클래스
 * @class Engine
 * @param {EngineOptions} options - 엔진 초기화 옵션
 */
class Engine {
  /**
   * 새로운 3D 오브젝트를 씬에 추가
   * @param {Object3D} object - 추가할 3D 오브젝트
   * @returns {Object3D} 추가된 오브젝트
   */
  addObject(object) {
    // ...
  }
}
